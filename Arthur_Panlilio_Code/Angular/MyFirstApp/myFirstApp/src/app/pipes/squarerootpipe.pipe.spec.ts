import { SquarerootpipePipe } from './squarerootpipe.pipe';

describe('SquarerootpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SquarerootpipePipe();
    expect(pipe).toBeTruthy();
  });
});
