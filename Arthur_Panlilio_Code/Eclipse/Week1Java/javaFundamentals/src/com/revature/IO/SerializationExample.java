package com.revature.IO;

public class SerializationExample {
	/*
	 * Turns object into a byte stream to be reverted back into an object.
	 * Serializable if class or superclass is serializable.
	 * saves instance variables.
	 * externalizable
	 * 
	 * State of the object, meaning its instance variables
	 * transient - means that instance var will not get serialized
	 * 
	 * deserializing objects w/ transient or static vars will
	 * hold the defualt value for that data type.
	 */
	public static void main(String[] args) {
		
	}

}
